<!--
  .cross-review.example.md
  ==========================================================================
  使い方:
  1. このファイルをリポジトリ直下に `.cross-review.md` という名前でコピーする。
  2. 下の <...> プレースホルダを、自分のプロジェクトで壊れやすい不変条件に置き換える。
  3. 汎用部分として `tools/cross-review.js` / `docs/cross-review.md` /
     `tests/cross-review.test.js` / `.cross-review.example.md` を取り込む。
     `tests/cross-review.test.js` の require パスだけ取り込み先の配置に合わせる。
     tools/*.js は CommonJS なので、ルート package.json が "type":"module" の場合は
     tools/package.json に "type":"commonjs" を置く。
  4. ルート package.json の scripts に追加:
       "review:codex":     "node tools/cross-review.js codex",
       "review:codex:fix": "node tools/cross-review.js codex --fix",
       "review:claude":    "node tools/cross-review.js claude"
  5. codex / claude のスタンドアロン CLI を PATH に通す (プラグイン/アプリとは別物)。
     リモートコントロール等で外部 CLI を spawn できない環境では、`node tools/cross-review.js subagent`
     が外部 CLI 無しでレビュープロンプトを stdout に出すので、それを Claude の客観サブエージェントへ渡す。

  観点の解決順 (loadChecklist):
    環境変数 CROSS_REVIEW_CHECKLIST(パス) → <cwd>/.cross-review.md
    → <スクリプト>/../.cross-review.md → 組み込みの汎用観点
  どれも無ければ汎用観点でレビューします (起動時に stderr へ警告)。
  スクリプト基準も候補にするので、cwd がリポジトリ直下でなくても
  (絶対パスや ../tools/cross-review.js 等で起動しても) 観点を拾えます。

  注意: この .md の全文がそのままレビュアーへの「観点プロンプト」になります。
  この HTML コメントは .cross-review.md へコピーした後で削除して構いません
  (残っていても無害ですが、観点本文だけにしておくのが綺麗です)。
-->

あなたは <プロジェクト名 / 一言説明> のコードレビュアーです。
以下の差分を、本リポジトリ固有の観点でレビューしてください。日本語で、各指摘に重大度
(blocker / 要修正 / 提案) を付けてください。問題が無ければその旨も明記してください。

重点的に見る観点:
- <壊れやすい不変条件・禁則をここに列挙する>
  例) 永続化フォーマットの後方互換性 / 特定 API の使用禁止 / 命名・配置の規約 /
      バージョン番号やキャッシュ名の同期 / セキュリティ上の禁則 など
- <観点2>
- <観点3>
- 構文/整合性: <このリポの lint / test / ビルド コマンド> を通る変更か。

該当しない観点はスキップして構いません。差分に現れていない既存挙動の壊れ (回帰) も疑ってください。
